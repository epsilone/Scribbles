This is the client.

It has to run on a php server.

It is connected by default to localhost, but can be configured to access a remote cloud9 instance (commented out in the code.)


Alternatively, the code can be find and run under: https://c9.io/ben_epsilone/ubi_test_php